# Single source of truth for the app version. app/build.gradle reads this.
# versionName: semver shown to users. versionCode: integer, bump every build
# that is uploaded/distributed (Android requires it to increase to update).
versionName=0.1.0
versionCode=1
